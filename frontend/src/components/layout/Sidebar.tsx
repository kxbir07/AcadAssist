import { NavLink } from 'react-router-dom';
import { Home, BookOpen, GraduationCap, FileText, CalendarDays, Sparkles, BarChart3, Settings, HelpCircle, LogOut } from 'lucide-react';
import { useApp } from '../../context/AppContext';
import { useAuth } from '../../context/AuthContext';
const primary=[['/dashboard','Dashboard',Home],['/courses','Courses',GraduationCap],['/knowledge','My Knowledge',BookOpen],['/assessment','Assessment',FileText],['/planner','Study Planner',CalendarDays],['/assistant','AI Study Assistant',Sparkles],['/progress','Progress',BarChart3]] as const;
const secondary=[['/settings','Settings',Settings],['/help','Help & Support',HelpCircle]] as const;
export default function Sidebar(){const {sidebarOpen,setSidebarOpen}=useApp();const {signOut}=useAuth();const logout=()=>{signOut();setSidebarOpen(false);};return <><div className={`sidebar-overlay ${sidebarOpen?'show':''}`} onClick={()=>setSidebarOpen(false)}/><aside className={`sidebar ${sidebarOpen?'open':''}`}><div className="sidebar-brand"><div className="brand-mark">A</div><div><div className="sidebar-brand-title">AcadAssist</div><div className="sidebar-brand-sub">Learn · Plan · Practice · Grow</div></div></div><nav className="sidebar-nav">{primary.map(([to,label,Icon])=><NavLink key={to} to={to} className={({isActive})=>`sidebar-nav-link ${isActive?'active':''}`} onClick={()=>setSidebarOpen(false)}><Icon size={18}/><span>{label}</span></NavLink>)}<div className="sidebar-divider"/>{secondary.map(([to,label,Icon])=><NavLink key={to} to={to} className={({isActive})=>`sidebar-nav-link ${isActive?'active':''}`} onClick={()=>setSidebarOpen(false)}><Icon size={18}/><span>{label}</span></NavLink>)}</nav><div className="sidebar-footer"><button className="sidebar-logout" onClick={logout}><LogOut size={15}/><span>Log out</span></button><div className="sidebar-footer-quote"><p>“Small sessions.<br/>Compounding progress.”</p><div className="quote-bar"/></div><div className="sidebar-version">AcadAssist · local-first frontend</div></div></aside></>}
